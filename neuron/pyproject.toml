[project]
name = "neuron"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "crewai>=0.130.0",
    "redis>=6.2.0",
]

[tool.ruff]
line-length = 120
